<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:elevation="8dp"
    app:cardCornerRadius="8dp">

    <LinearLayout
        android:id="@+id/idLLProducts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:orientation="vertical">

        <!-- Text view para o nome do produto -->
        <TextView
            android:id="@+id/idTVNomeProduto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="Nome do produto"
            android:textColor="@color/black"
            android:textSize="15sp" />

        <!-- Text view para preço do produto -->
        <TextView
            android:id="@+id/idTVPrecoProduto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="Preço do produto"
            android:textColor="@color/black"
            android:textSize="15sp" />

        <!-- Text view para a descrição do produto -->
        <TextView
            android:id="@+id/idTVDescricaoProduto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="Descrição do produto"
            android:textColor="@color/black"
            android:textSize="15sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!--Botão de edição-->
            <Button
                android:id="@+id/idBtnEdit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Clique para alterar"
                android:onClick="onEditClick" />

            <!-- Botão de edição e exclusão -->
            <Button
                android:id="@+id/idBtnDelete"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Deslizar para excluir"
                android:onClick="onDeleteClick" />
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>
